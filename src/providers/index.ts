import FinancialReportProvider from "./api/FinancialReportProvider";
import LoginProvider from "./api/LoginProvider";
import CorrectionProvider from "./api/CorrectionProvider";

const providers = {
  FinancialReportProvider,
  LoginProvider,
  CorrectionProvider
};

export default providers;
